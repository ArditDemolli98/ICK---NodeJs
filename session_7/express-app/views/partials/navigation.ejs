<header>
    <nav>
        <ul>
            <div class="left-nav">
                <li><a class="<%= title === "Home" ? "active" : "" %>" href="/">Home</a></li>
                <li><a class="<%= title === "About" ? "active" : "" %>" href="/about">About</a></li>
            </div>
            <div class="right-nav">
                <% if(isAuthenticated) { %>
                <li><a class="<%= title === "Add product" ? "active" : "" %>" href="/admin/add-product">Add Product</a></li>
                <% } %>
                <li><a class="<%= title === "Login" ? "active" : "" %>" href="/login">Log in</a></li>
                <form action="/logout" method="post" class="logout-form">
                    <button class="nav-link">Log out</button>
                </form>
            </div>
        </ul>
    </nav>
</header>